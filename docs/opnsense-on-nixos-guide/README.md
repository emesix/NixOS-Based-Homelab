# qotom-router docs bundle (template-aligned)

This folder matches the `wiki/infra/hosts/<host>/` template with `_index.md` as landing page,
and subfolders: `runbooks/`, `howto/`, `ops/`. Supporting assets live under `assets/opnsense/`.

Push into your repo root so the paths resolve in Wiki.js.
