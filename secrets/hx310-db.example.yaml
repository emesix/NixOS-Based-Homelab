# Example structure (encrypt with sops)
postgres:
  password: "choose-a-strong-password"
vaultwarden:
  admin_token: "generate-a-random-64-char-token"
gitea:
  oauth_secrets: "if-needed"
wikijs:
  db_url: "postgres://postgres:${postgres.password}@10.42.20.20:5432/postgres"
